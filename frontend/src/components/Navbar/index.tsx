import * as React from 'react';
import {AppBar, Toolbar} from '@material-ui/core';

export const Navbar: React.FC = () => {
  return (
    <AppBar>
      <Toolbar>Teste</Toolbar>
    </AppBar>
  );
};
